#pragma once

#include "SceneManager.h"
#include "TitleScene.h"

void LoadScene()
{
	/*auto TitleSceneNumber = GEngine::SceneManager::CreateScene<TitleScene>(L"TitleScene");

	GEngine::SceneManager::LoadScene(TitleSceneNumber);

	auto s1 = GEngine::SceneManager::GetScene(TitleSceneNumber);*/
}